const { slots } = require("../data");

const state = {
  slots,
  error: false,
  errorMessage: "",
  success: false
};

const mutations = {};

const actions = {};

export default {
  state,
  mutations,
  actions
};
